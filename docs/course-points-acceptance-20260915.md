# 課程點數、共卡與排課 Preview 驗收

日期：2026-09-15（Asia/Taipei）。Draft PR #1022，分支 `codex/course-scheduling-stage1`。
核心流程驗收版本：`3f6c00278b4802bdf5db259a3bc22c669abf8153`。
最終程式版本：`4e19eda3`（僅追加月曆版面修復），Vercel READY：`AvZtKFMguKBvQqM7EaSisy9Gpa3j`。
後續文件提交不改動程式；實際部署版本以 PR 最新 Vercel check 為準。

Preview：[課程完整驗收店 0915](https://steamfoot-booking-git-codex-cour-7f6935-rock7652-2111s-projects.vercel.app/s/course-e2e-20260915)。由專屬入口的「後台登入」登入店長；會員使用手機登入。測試密碼不存入 repository。

## 範圍與資料隔離

- 接續既有首頁、停用排序、多日期複製等成果；未合併、未部署正式站、未修改正式資料庫。
- 僅使用 Supabase `steamfoot-preview` (`ttworfzgwejdeolegkxl`)。Preview build preflight 核對兩個資料庫 URL 的專案及必要資料表／欄位。
- 新店 `course-e2e-20260915`，新增顧客 A/B、教練、受限店長、教室、課程、共卡與現金帳，全部保留供重驗。
- 原店 `course-qa-20260915` 最後核對仍是 **9 堂排課、3 間教室、4 個課程**，本輪未改動其資料。
- 體驗版原有 3 名人員、100 名顧客、100 筆月預約限制保留。第 4 名啟用人員的網頁新增請求被拒絕，沒有半筆建立結果。
- 登入帳號及過期邊界卡由隔離 SQL 建立；下述顧客、方案、指派、共卡、排課、人員、收支、購買及點名均為真實網頁操作。SQL 用於交叉核對結果，不冒充網頁驗收。
- 原 Chrome 連線中斷後改用 Codex In-app Browser 成功完成後續流程，未要求重新登入失效視窗。

## 已完成實作

- 課程專用首頁、排課、課程／教室清單、人員、顧客、點數方案、營運、分析與設定入口；同頁側窗及固定操作區。
- 課程分類選填、預設教室選填、更多內容；上下架／教室隱藏置底淡化、可恢復，保留歷史與使用提示。
- 月曆每日最多兩堂及「＋N 堂」，月份切換保留篩選；多日期複製、每週截止日預覽、單堂及後續系列修改，教室／教練衝突阻擋整批交易。
- 顧客及點數方案建立／編輯、期限、指派、授權共卡；明確顯示操作人與上課人、自己上課與代約。
- 預約占用、取消釋放、點名扣點與異動紀錄在同一交易中更新；防止超額、重複預約、到期、點數不足及重複扣點。
- 店長信箱／密碼與適用店內權限；教練不建立後台密碼，以固定會員身分連結進入「我的工作」。教練／會員雙身分或僅工作模式可切換，停用撤銷工作權限而保留顧客與歷史。
- 沿用中央身分解析及 StaffMemberLink，不以同姓名／電話直接授權；修正中央解析拒絕後的舊連結繞過。
- 設定店名及預約／取消提前分鐘；分析使用實際排課、預約與出席紀錄。沒有新增取消收費規則。
- 共用分店選單有搜尋、限制高度、背景捲動鎖定、清單邊界處理，並修復收合側欄無法展開選單的程式問題。

## 真實核心流程與資料證據

1. 店長建立 A、B，新增「0915 共卡十點」10 點／1000 元／90 天，指派 A 並加入 B；到期日 2026-12-13。
2. 建立容量 2 的教室、60 分／3 點／上限 1 的課程（不指定預設教室），建立並連結教練 A。
3. A 登入只替 B 預約 9/15 21:05 課程：只有 B 一席，顯示操作人 A、上課人 B、共卡代約；剩餘 10、占用 3、可用 7。
4. 取消後席位及占用釋放，剩餘仍 10；重新預約。B 登入看到相同紀錄，沒有未授權的工作入口。
5. A 的「我的工作」只顯示授權課程名單；課前點名被伺服器拒絕。21:05 後成功出席，唯一 DEBIT 3 點，剩餘 7、占用 0。
6. A 代約 B 9/16，B 反向代約 A 9/17：占用 6、可用 1。再約 9/18 被拒絕點數不足，SQL 該次零預約。
7. 專用卡先有效，在已開啟預約的畫面外由 SQL 將該 fixture 到期時間改為昨天；舊畫面送出仍被拒絕。零預約、零占用／扣點，入點 10 保留。前台显示已到期、可用 0 並排在正常卡後。
8. 店長取消整堂 9/16，確認前顯示影響 1 人。SQL 課程取消、預約 CANCELLED、唯一 RELEASE 一致；取消歷史保留。另一筆未來預約亦取消，回到占用 0。
9. 最終版本新建 30 分／2 點／上限 2 的課程，9/15 22:10 開課。A 只替 B 預約；尚有空位時重複預約仍被拒絕。取消釋放 2 點，重新預約。
10. 在 **兩個真實瀏覽器視窗**同時開啟待點名名單。第一視窗出席成功，再從尚未更新的第二視窗重送點名，兩邊均顯示已出席，SQL 只有一筆 DEBIT 2 點。
11. 最終共卡 **剩餘 5、占用 0、可用 5**；總扣點 5（3+2）。前台 B 顯示兩次出席、取消紀錄、操作人與點數明細；後台今日 2 堂／2 人／未點名 0。九月分析 7 堂／預約 2／出席 2；十月 1 堂／0／0。

關鍵 fixture：共卡 `cmu2o78ek0002js0424w5z8su`；最終課程 `cmu2qd0j20003k00420kmx09o`；過期卡 `course-e2e-expiry-card-0915`。

## 排課、權限及營運實際驗收

- 多日期輸入曾在實際操作中清空；修正 FormData 與固定欄位 key 後，9/16、17、18 三堂同批成功。混合撞期批次逐日顯示衝突並禁止送出，沒有部分成功。
- 星期四週課 9/17、9/24、10/1 成功；「這堂及後續」由 20:00 改 20:15，SQL 三筆一致。移到既有 21:05 課程撞期時整批拒絕。
- 月曆 9/17 三堂只顯示兩堂加「＋1 堂」；房間容量 2 而排 3 人有即時提醒。
- 隱藏教室顯示仍使用的課程，既有課保留，新排課不提供該教室；還原啟用。
- 下架三點課後置底，既有課仍在月曆，新排課只可選二點課；還原上架後恢复正常排序。
- 受限店長關閉新增預約、編輯顧客、管理人員後，實際登入確認操作入口受限。跨店 URL 修正後重測為 404。
- 教練停用後 Staff INACTIVE、連結 revoked，但會員 User ACTIVE；會員仍有卡及歷史、工作入口消失。恢復後可工作。
- A 改為僅教練模式，實際只見自己的工作、不見會員方案及其他教練週課；恢復雙身分。
- B 顧客頁「加入為教練」連結原 B User，顧客仍只有兩筆。測完停用 B 教練，會員身分保留；受限店長恢復啟用，課程店有效人員仍為 3。
- 設定預約 15／取消 30 分鐘成功儲存，後續以網頁恢復 0／0；沒有附加費。
- 現金抽屜開店 1000，新增收入 20；營運同頁改為 25，首頁同步 1025。以 1025 閉店成功，差額 0，留下完整收支及閉店快照。

## 桌機與 iPad 尺寸

- 真實 Chrome 及 In-app Browser 網頁，1024×768：人員側窗儲存按鈕 top 708／bottom 752，高 44；背景 overflow hidden。
- 十月完整月曆最後一列 bottom 731，小於畫面 768，沒有文件橫向溢出；換月份保留教練篩選。
- 分店清單搜尋「課程完整」成功篩選。畫面驗證獨立捲軸；鍵盤聚焦最下方分店後清單內部捲動，背景課程表位置保持，搜尋欄保留在頂部。
- 最後截圖發現三堂日期的文字因 flex shrink 被壓在一起，已修復固定行高及縮小工具列留白。於 `4e19eda3` 真實 Preview 重測：九月三堂格文字不重疊，八月六週月曆完整落在 1024×768 畫面內。這兩筆提交只調整月曆 CSS，無資料或預約邏輯變更。
- 此為桌面瀏覽器 iPad 尺寸驗證，**不是實體 iPad Safari 觸控慣性／邊界滑動驗收**。

## 蒸足／SPA 實際回歸

只新增本次專用顧客與交易，不修改既有客戶或服務設定。

- 蒸足 `staging-store`：新顧客「課程0915蒸足回歸顧客」，店長以匯款購買原 10 堂套餐 3000 元，發放 10 堂；使用原週四時段建立 9/17 10:00 預約，10 剩餘／1 占用／9 可約；完成服務後 9 剩餘。SQL Booking COMPLETED，WalletSession 9 AVAILABLE、1 COMPLETED。預約 `cmu2rgyht0003l504ili2akax`。
- 蒸足原有 6/17 未閉店抽屜未更動。最初以無 Staff/storeId 的臨時 ADMIN 購買被拒絕「缺少 storeId」，未發卡；改用專用 OWNER 成功。此為既有總部購買上下文限制，不宣稱修復了該額外流程。
- SPA `store-spa-module-qa-20260903`：新顧客「課程0915SPA回歸顧客」，以轉帳買原芳療兩次方案 3000 元；建立全身芳療預約、完成並以方案扣次結帳。UI 顯示已完成／已結帳、剩 1 次；SQL Booking COMPLETED、Entitlement remainingUses=1、唯一 COMPLETED use=1。預約 `cmu2rlkou0001l504ffd3w4vz`。
- 測完停用兩個臨時 OWNER／Staff 及臨時 ADMIN，保留交易參照。原蒸足／SPA 有效人員數恢復 2／4；未撤銷本輪課程驗收店帳號。
- 此回歸涵蓋上述購買、預約及扣堂／扣次，未窮舉每種退款、混合付款、訂閱或通知路徑。

## 程式與部署檢查

- 課程服務／排課／身分針對性測試 32 項通過；TypeScript 與 ESLint 通過。
- 最新程式 commit 的 GitHub Changed-file ESLint、Targeted tests、Typecheck、Full Vitest baseline 全部通過，Vercel Preview 部署完成。
- `postgres-integration` job skipped，不能當作通過。實際 Preview SQL 及網頁證據如上。
- 原有 Cloudflare Workers build check 仍失敗；本輪未操作正式部署，不以 Vercel Preview 成功聲稱所有 CI 全綠。
- 新 migration 僅套用 Preview：課程目錄擴充、點數／共卡／預約／ledger／規則、StaffMemberLink.courseMemberEnabled。正式 migration allowlist 未增加。

## 交付限制與待人工驗收

- 實體 iPad Safari 的手指滑動與慣性邊界仍須在裝置上確認；現有工具只完成尺寸、鍵盤可達性、獨立捲軸及背景位置檢查。
- 已驗證第二視窗重送點名，未進行真實多使用者同一毫秒的壓力競爭；此部分有交易鎖及自動測試，不能等同真實負載驗收。
- 跨店後台 URL、角色停用及 UI 權限實測通過；惡意偽造未授權上課人／跨店 Server Action payload 由伺服器檢查與測試覆蓋，未以真實瀏覽器篡改 payload 驗收。
- 教練工作登入需固定連結已完成註冊的會員帳號；僅工作模式會隱藏會員功能。本輪沒有新增獨立教練邀請／OTP 發送流程。
- 堂數／期課／訂閱、自動續費不列已完成；沒有以無作用按鈕表示支援。
- 等使用者自行驗收並明確同意合併；仍維持 Draft PR，禁止自行正式上線。
