# 課程模組回饋驗收 — 2026-09-16

分支 `codex/course-scheduling-stage1`，Draft PR #1022。接續遠端 `2d333e54`，保留既有成果及使用者資料。

本輪最終程式版本 `3d7eb8839470792fae34b81aaa202b5405857c14`，Vercel READY `dpl_6g8fF5MYRa5VkiEhzU9xTJzHE23F`。後續文件提交不改程式。

## 入口與資料邊界

- [課程回饋驗收店 0916](https://steamfoot-booking-git-codex-cour-7f6935-rock7652-2111s-projects.vercel.app/s/course-e2e-20260916/)。入口「後台登入」使用店長信箱；會員用手機登入。密碼另交付，不寫入公開 repository。
- 店長：`course-e2e-0916@example.test`。會員 A：`0999160002`，會員 B：`0999160003`。限權店長：`course-limited-0916@example.test`。
- 全部寫入僅在 Supabase Preview `ttworfzgwejdeolegkxl`。沒有操作正式資料庫、合併或部署正式站。
- 獨立建立 0916 店；原使用者測試店仍為 9 堂排課、3 間教室、4 個課程。0915 驗收課程、教室、上下架與點數資料未改動。
- 新增 migration `20260916024857_course_attendance_and_staff_contacts` 僅套用 Preview。部署 preflight 核對課程報到、備註及人員聯絡欄位；正式 migration allowlist 未擴張。
- 登入帳號、固定顧客帳號綁定與到期卡是 SQL 測試準備。顧客、教練、店長權限、健康量測、方案、指派、共卡、教室、課程、複製、預約、點名、收支與閉店均實際操作網頁。

## 蒸足成熟功能沿用與課程對應

| 項目 | 沿用／對應 | 課程資料及權限 |
| --- | --- | --- |
| 整體 | 共用 Logo、sidebar、PageShell、綠金色系、RightSheet、固定操作區 | 課程樣式限於 course-workspace／course-home；保留各模組導航 |
| 顧客詳細 | 共用顧客驗證 schema、BirthdayFields；姓名、電話、email、性別、生日、身高、LINE 名稱、服務備註 | 本店 Customer；課程預約、方案及共卡接 Course 資料表，不讀蒸足扣堂 |
| 健康追蹤 | HealthAssessmentCard、原生健康摘要、指標定義、曲線、量測驗證及 BMI 計算 | 嚴格 storeId + customerId；查看／新增／編輯由 customer.read／update 控制 |
| 名單 | 沿用蒸足「顧客服務備註」與「本次預約備註」語意，側窗操作 | 實際上課人、預約操作人、方案名稱、可用點數、期限、報到／完成／未到 |
| 方案 | 清單、上下架、同頁編輯及指派、方案摘要及異動紀錄 | CoursePointPlan／Card／Member／Entry；沒有套用 WalletSession 或 SPA entitlement |
| 現金與營運 | 原 CashDrawerWorkspace、收支表單、編輯及篩選 | 共用店家 cashbook 權限與範圍；首頁現金置頂，課程置後 |
| 分析 | 共用月份操作及成熟 TrendChart | 真實排課、去重學員、參與人次、完成、報到待完成、未到、扣點、實際收支 |
| 設定 | 店名、地址、地圖、官方 LINE；可保存 | 課程專用預約／取消提前分鐘；未接蒸足服務時段、扣堂、空間費、SPA 付款流程 |
| 人員 | 共用側窗及固定帳號連結模式 | 姓名、電話、緊急聯絡人／電話；店長登入與分組權限，教練前台工作、可兼顧客 |

方案入口原本受到蒸足 PLAN_MANAGEMENT 方案功能門檻影響；課程入口改按已完成功能開放，仍檢查 wallet.read，原方案額度保留。使用方案名稱取自 CoursePointCard.nameSnapshot，成員名稱取自本店 Customer；現在明列點數單位、可用點數及到期日，過期／不足／不涵蓋上課日不能選。未以隱藏資料或原始 ID 替代名稱。

## 核心網頁驗收

1. 建立 A／B，A 加入教練並固定連結會員；儲存電話與緊急聯絡人。建立限權店長，20 項店內權限預設開啟，取消 booking.create、customer.update、staff.manage，保存為 17 項。
2. 建立「0916 共卡十點」10 點／1000 元／90 天，指派 A（2026-12-14 到期），加入 B 共卡。
3. 建立容量 2 教室與 30 分／每人 3 點／上限 1 課程，排 9/16 11:30；同批複製至 9/17、9/18，另複製 9/19。
4. A 自己不上課，只替 B 預約 9/16：後台同頁顯示 B、操作人 A、共卡代約、三點方案、期限、顧客服務備註及本次備註；剩餘 10／占用 3／可用 7。
5. 取消後唯一 RELEASE 3、占用歸零；重約後唯一 RESERVE 3。報到只寫 checkedInAt，仍 RESERVED，沒有 DEBIT，剩餘仍 10。
6. 兩個真實瀏覽器預約同堂最後一席：只有 B 成功；同時使用共卡最後可用點數預約不同課：只有一筆成功。交易鎖下沒有超額席位或點數；失敗後名額及餘額重新載入。
7. 為驗證未到，只將本輪複製的一堂 fixture 移到當日 10:00–10:30；未到操作透過網頁執行，狀態 NO_SHOW、唯一 RELEASE 3、沒有扣點或收費。
8. 11:30 開課後，兩個真實視窗同時提交同一筆「完成並扣點」：僅一筆 DEBIT 3，狀態 ATTENDED。兩邊可重送而不再扣點。
9. 取消另外競爭測試的未來預約。最終主共卡剩餘 **7／占用 0／可用 7**。A、B 前台與後台、點數紀錄一致；B 看到 A 代約及本次備註，沒有教練工作入口。
10. 分析：4 堂排課、去重 1 位學員、2 參與人次、完成 1 人次、未到 1 人次、扣點 3、待完成 0；包含未來尚未上課的排程，已標示統計口徑。

核心 fixture：主卡 `cmu3ix6rl0002gt04u6qo4nxp`，主課 `cmu3izx4c0007gt049fmbzqpm`，完成預約 `cmu3j6h7h0009if04a3svc6p1`。四筆預約最終為 ATTENDED、NO_SHOW、CANCELLED、CANCELLED；每筆各一筆 RESERVE，完成一筆 DEBIT，其他各一筆 RELEASE。

## 其他實際操作與修復重測

- B 新增量測 60 公斤／體脂 24%，再編輯為 61 公斤及編輯備註；重新打開仍為 61。A 沒有 B 的量測；限權店長可讀但無新增／編輯。跨店網址回傳 404；偽造跨店 record ID 的後端拒絕另有自動測試。
- 停用 A 教練後前台工作入口消失，會員、卡及歷史保留；恢復啟用。人員資料與聯絡人保存正確，停用列置底；限權店長沒有新增／編輯人員、沒有新增預約表單。
- 方案下架後禁止指派，已指派主卡與歷史保留；恢復上架。另備到期邊界卡：剩餘 10，但可用 0，前後台明示已到期，預約選項禁用且顯示期限。
- 首頁開店 1000，網頁新增收入 200，營運同頁改為 250；首頁與分析收入 250、抽屜 1250。發現閉店摘要漏列手動收支，修復後重測顯示 250；實點 1250 閉店，差額 0、下次起點 1250。
- 發現 PageShell 覆寫 className 丟失既有排版、同月換日導致側窗重置、競爭預約失敗餘額沒有即時更新、iPad 現金表單過窄，均修復並在 Preview 重測。
- 本輪瀏覽器 1024×768：綠金清單、課程點選即展開、整月精簡格、側窗高度 768、背景 overflow hidden、文件無橫向溢出。現金表單展開後使用可讀寬度。尺寸控制改 768×1024 時工具回報仍 1024×768，因此不將直向列作已驗證。
- 蒸足／SPA 本輪做受共用元件影響的實際讀取回歸：蒸足首頁、預約側窗、顧客方案仍已完成、9／10 堂；SPA 首頁、排程側窗仍已完成／已結帳。原購買／扣堂完整寫入流程在 [0915 紀錄](course-points-acceptance-20260915.md)，本輪未重做所有購買付款組合。臨時回歸 OWNER／Staff 已恢復停用。

## 檢查與限制

- 本地課程／排課／權限／隔離針對性 57 項、健康 5 項通過；閉店修復後現金服務與 live totals 67 項通過。TypeScript、變更 ESLint 通過。
- `3d7eb883` CI：Changed-file ESLint、Targeted tests、Typecheck、Full Vitest baseline、Vercel 全部通過。
- `postgres-integration` job skipped，未視為通過。真實 Preview 資料庫與網頁交易已核對，但不冒稱該 job 已執行。
- Cloudflare Workers build failure 依使用者明確同意排除本輪合併門檻。
- 沒有實體 iPad Safari；手指觸控慣性、旋轉及邊界手勢尚待實機驗收。完成的是瀏覽器尺寸／側窗／獨立捲動結構檢查。
- 同時搶位與點數使用兩個真實視窗，不宣稱已完成大量併發壓力測試。沒有以瀏覽器篡改未授權 payload；跨店、權限及 idempotency 的後端測試仍保留。
- 健康編輯清單、顧客課程歷史及點數異動目前取最近 100 筆；歷史資料保留，未提供更早頁面載入。
- 堂數、期課、訂閱及自動續費仍為後續；不提供無作用選項。教練工作需連結已註冊固定會員帳號。

維持 Draft，等待使用者驗收並明確同意合併。沒有自行正式上線。
