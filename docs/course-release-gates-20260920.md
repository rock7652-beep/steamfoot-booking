> 2026/09/20 最新決策：停止額外備份匯出／Docker 還原及憑證索取，取消相關發布門檻。正式執行狀態、CI 與自動審核阻礙以 [最新發布紀錄](course-production-release-status-20260920.md) 為準。以下保留歷史證據，不再作為重複索取授權或備份憑證的依據。

# 2026/09/20 收尾：出席、HQ 升級、通知與正式發布

## 固定範圍

A 店仍為唯一日常驗收店。不得調整其方案、期限、上限或直接寫餘額。本人實體 LINE 登入／預約／取消已通過，不重跑、不美化。專用內部隔離店承接到期與三方案升級，不能以 A 店改方案測試。Draft；沒有正式資料寫入、合併或正式部署授權。指定单則訊息已獲授權並完成，不再外發。正式唯讀備份已授權，但缺既有連線，演練尚未執行，憑證盤查已停止。

## 出席閉環

A 店既有9/20 10:00堂數卡課次、11:00點數卡課次，達既有服務允許時間後才操作；不修改系統時鐘或課程時間。

1. 讀取預約、卡片、占用與異動基線，確認報到未扣抵。
2. 透過既有授權操作出席，堂數卡1堂、點數卡依課次成本；狀態、餘額與紀錄同時核對。
3. 重送同一請求，確認無第二筆扣抵；只點另一個按鈕不冒充相同請求重送。
4. 依既有更正规則返回待上課，驗證正式扣抵返還一次、有效預約恢復占用（可用額度不一定隨返還而增加）。
5. 同一更正請求重送，不新增返還；前後台與紀錄一致。

### 10:00 堂數卡實測（10:05–10:10）

- Chrome A 店長於固定驗收網址點選既有測試學員「出席」，RESERVED → ATTENDED；剩餘4→3堂，DEBIT僅1筆。
- 以既有私有店長測試會話向同部署正常 Server Action 提交相同 booking/status 請求兩次，皆成功；沒有直接改資料庫。這是 HTTP 相同參數重送，不是宣稱擷取了第一次瀏覽器的完整網路封包。
- 以既有已連結教練測試會話提交 saveCourseAttendance，target=RESERVED、expected status=ATTENDED；完全相同 body 連續提交兩次，兩次均明確回傳 success:true。
- DB 最終：RESERVED、remaining=4、held=1、available=3；保留RESERVE 1、DEBIT 1與唯一CORRECT:ATTENDED:RESERVED +1，不重扣、不重返。
- Chrome 店長重開名單：未報到／占用1堂、可用3堂；更正按既有規則清除報到狀態。測試學員前台 HTTP SSR 資料同樣remaining4/held1/available3且有相同更正紀錄。本次沒有再做本人實機登入／預約／取消。
- 限制：教練更正與重送本次使用已授權 HTTP 交易驗證，非實體手機點擊；11:00點數卡課次本次未重跑，沿用既有未受修改影響的點數出席證據，不標成A本次點數閉環通過。

## HQ 升級操作矩陣

隔離 HQ 網址為固定 preview 的 `/hq/login`，必須 ACTIVE ADMIN；A 店 OWNER 無權跨店調方案。私有登入資料保存在本機，不寫入本文件／PR。

專用隔離店與店長透過正常 HQ 建店；課程、卡片、預約及待核帳購買為明確標示的合成資料保留 fixture，另建立到期訂閱 fixture，不能作為真實購買／發卡驗收證據。保存關聯的前後雜湊與筆數。每次升級 BASIC、GROWTH、ALLIANCE 都須檢查：

- 到期：既有資料仍可讀，受限寫入按原規則拒絕；不刪帳號、卡片、預約。
- 升級：同 Store ID、同登入帳號及會員關聯，新的現行訂閱與方案用量生效，寫入恢復。
- 資料：Customer／Staff／身分連結、課程、卡片、持卡成員、預約、額度異動、購買／退款及現金交易不被重建或重算。
- 訂閱歷史與 PLAN_ACTIVATED 紀錄可追溯；舊登入保持合法權限，不取得其他店資料。
- 最後唯讀比較 A 店方案、期限、額度與身份，確認完全未受影響。

### 本次 HQ 瀏覽器結果

已取得隔離HQ登入；使用專用內部隔離店，未切換使用者日常A入口。

|操作|實際結果|
|---|---|
|到期體驗店原店長登入|成功讀取既有教室；顯示到期唯讀|
|新增教室送出|後端拒絕唯讀，草稿保留|
|HQ改基礎版ACTIVE|原店長會話仍有效；同草稿再次儲存成功，新增一間驗收教室|
|HQ改專業版ACTIVE|保存成功，原有資料雜湊一致|
|HQ改展店版ACTIVE|保存成功；原店長頁顯示展店版、兩間教室保留|

`course-lifecycle-retention-20260920.json`：12類專用店原始資料與A方案快照，三次升級皆完全一致。新增加的寫入恢復驗收教室單獨排除於原始教室雜湊，未移除資料。A仍體驗版、原期限、原LIFF及現行訂閱。

驗收界線：本次是原店長登入、到期限制、HQ三方案更新、資料保留與寫入恢復；合成購買為PENDING，無退款／現金／健康／LINE關聯fixture，因此不宣稱上述各類升級資料均經本次實測。各版功能矩陣與額度邊界沿用既有測試，未逐項重新操作全部功能。

## LINE 通知驗收：已通過並收尾

指定單則測試Flex已使用本店通道發送；使用者本人手機確認收件、文字／按鈕呈現，以及返回A店本人9/21 10:00伸展瑜珈預約一致。相同請求重送被略過，成功紀錄仍一筆。臨時外發授權已撤除，A店自動提醒維持關閉，不重送。

Messaging API與Webhook已依授權設定；Console Verify成功。原客服中心及其他店家未變更。詳細證據見 `course-line-flex-acceptance-20260920.md`。

此證據不等同18:00 cron自動觸發、其他提醒事件、follow/unfollow實機通過。上述限制明列，不以此重新啟用提醒或外發。

## 正式备份與恢復準備

2026/09/20 唯讀 Supabase Console `steamfoot` 正式專案，Scheduled backups 列出7份PHYSICAL，最新 `2026-09-19 17:23:31 +0000`（台灣9/20 01:23:31）。PITR 頁仍為 Enable add-on，未啟用。本輪沒有按 Restore、沒有新資源／付費操作。

- 已證明：備份清單可用；15份隔離 SQL 故意失敗完整回滾。
- 尚未證明：該正式備份成功恢復、恢復後應用可登入／交易、正式資料量下遷移鎖等待與耗時。
- 日備份不涵蓋之後交易；DB 備份不含 Storage 實際檔案。不可直接以舊備份覆蓋正式庫並宣稱無損。
- 已核准既有無網路Docker中的新空DB `course_prod_restore_20260920`，已建立空目標。狀態：**缺少既有正式連線，尚未執行**；没有正式資料副本、不新增費用。
- 憑證搜尋依使用者指示停止；不新增工作流程或入口取得secret。日後取得既有連線後，才依已核准方案執行一致性dump、還原、關聯／權限／必要功能驗證及耗時記錄。不能把合成資料演練視為真實備份恢復。

官方範圍：https://supabase.com/docs/guides/platform/backups

## 核准正式發布後才執行的順序

1. 固定核准 SHA、15份原始SQL雜湊與組裝交易包（第14份內層BEGIN/COMMIT已在演練明確處理），對正式schema差異重檢；不以跨目錄檔名排序。
2. 確認最新備份／恢復證據、維護窗口及寫入暫停安排；列明可接受資料損失與中止條件。記錄原应用可回退版本，先確保舊應用相容新增結構。
3. 執行核准遷移，設定鎖／語句逾時；任何錯誤ROLLBACK並停止發布，不重跑未知部分狀態。對已套用／部分存在的結構先查明，不跳過掩蓋。
4. schema／RLS／約束／遷移歷史核對通過後部署配對版本。資料庫不可缺表就先部署。
5. 發布後檢查：健康／版本、店長登入及店別、會員登入／教練資格、方案與預約讀取、指定受控交易、錯誤率、權限與隔離；蒸足／SPA受影響登入與原流程回歸。正式測試交易另需批准，不向真實會員發訊。
6. DDL提交前失敗：完整回滾。提交後應用故障：停課程新寫入、回退相容程式，保留新增結構與新交易，前向修復。不得直接DROP或回放舊備份抹去新交易；灾難恢復另走已核准程序。

15份順序與雜湊：`course-full-migration-rehearsal-20260920.json`。正式執行授權尚未取得。本文件是操作準備與界線，不是正式執行／恢復通過證據。
