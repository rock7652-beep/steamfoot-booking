# 課程介面第三輪：結帳、名單與刪除

## 授權與範圍
使用者已授權集中實作，驗收通過後合併正式站。沿用同一 A 店；保留店家方案、期限與會員關聯，不發送新測試通知。

## 已實作
- 指派方案同時結帳：金額／百分比折抵；現金、轉帳、信用卡、其他。轉帳必填後四碼；實收零記為全額折抵，不新增收入。發卡、購買、收款同一交易；請求重送不重複發卡或收款。
- 現金沿用開啟現金抽屜規則，退款與作廢辨識原收款方式。信用卡僅登錄收款，沒有新增支付串接。
- 顧客購買／核帳／退款、預約出席紀錄每次十筆，查看更多。
- 當日課表按時間顯示第幾堂；學員名單每頁十人，方案備註收合，取消名單分開。全選跨頁全班，批次報到、出席、未到、更正待點名。
- 課程、教室、人員、方案可勾選刪除；店長限定，顯示名稱與數量確認。任何使用關聯阻擋整批刪除；保留歷史及登入帳號。人員須停用，不能刪除本人／店長或已有會員關聯的人員。

## 驗證
- TypeScript、新增與受影響重點模組 ESLint 通過。
- 全測 597 檔／5242 項通過；資料庫專用測試於本機未設定連線時跳過，另由既有隔離 CI 執行。
- 新增 20 人精簡名單／全選批次互動測試通過。
- 新增真實 PostgreSQL 測試：重送只發卡收款一次、寫入失敗整筆回滾、零元無收入、混合使用中項目整批拒刪。
- checkout 加法遷移已套用隔離專案 ttworfzgwejdeolegkxl；正式尚未套用。來源檔為 20260920173000_course_assignment_checkout。
- 既有計酬遷移已套用隔離庫，兩份正式遷移與 Prisma 歷史尚待发布时核對。

## 尚未通過的門檻
- 新版本 CI／PostgreSQL 結果、Vercel 預覽待推送後取得。
- 瀏覽器既有原生確認視窗仍造成 CDP refresh tabs 逾時；本輪重試一次仍失敗。未強制關閉草稿分頁，實頁驗收不能宣稱通過。
- 實體 iPad／Safari／LINE 未測部分保留。Cloudflare 沿用既有豁免。
- 使用者已授權合併；驗收尚未完整通過，因此尚未合併或正式部署。
