# 課程手機版面：待使用者確認的隔離示意

本輪新增 `public/course-mobile-review/` 靜態 HTML/CSS/JS 與假資料截圖；Proxy 只為該目錄列名檔案開放 Preview 存取，正式環境回傳 404，不放寬其他路徑。既有 Next.js 功能頁、API、交易、共卡、權限、資料庫均未修改。使用者確認操作模式後才實作正式功能頁。

## 來源及限制

- 會員依據：歷史對話 course-member-colors.html（台北 2026-09-15 07:52:18）及 course-grouped-bookings.html（08:04:42）；詳見前台設計追溯文件。
- 未取得原始 HTML。左欄明確標示「原稿依據・操作結構整理」，是重製示意，不是原檔、像素還原或原稿截圖。原始來源連結保留，含私人對話的截圖不公開。
- 「目前版」也是 b4fc7cbb 的結構轉錄，用相同假資料情境比較，不冒充真實功能頁的截圖。
- 教練原稿未找到，修正版依使用者確認需求整理。
- 本輪訊息沒有附上所稱五張新截圖；依七項文字回饋、既有截圖及元件程式繼續處理。

## 對照與示意情境

入口 `/course-mobile-review/`。可切會員三欄／教練兩欄、375／390／430px。全欄同為 2026/09/16，09:00 已完成、18:00 本人及 B 同堂、19:30 只替 B 預約，C 取消與未到保留。

- 模式切換移至月曆上方，標明店家堂數／本人與共卡預約課次。
- 精簡店家／帳戶頁首；日格改成窄螢幕七欄 minmax(0,1fr)，沒有隱藏水平溢位、禁用縮放或縮小整頁。
- 日期下方立即顯示當日清單；點日期帶到課程。主畫面保留時間、課名、實際上課者、狀態與操作。
- 預約明細可展開，完成／未到與取消歷史分開預設收合。同一課次按學員逐人處理。
- 教練使用原位展開名單，保留選定日期；主要摘要堂數／人次，去重人數放統計說明。
- 可操作示意：逐人取消、本人／共卡勾選新增、報到、完成、未到、名單收合、歷史展開、模式與日期切換。重載重設所有假資料。
- 完全沒有 fetch、API、localStorage 或資料庫呼叫。示意點數文字不代表真實交易驗收。

## 已檢查（仅示意）

- 會員 375、390、430px：DOM document scrollWidth 不超過 innerWidth，七天與按鈕完整。
- 教練 375、390、430px 與 1024px：無頁面橫向溢出；名單原位展開，按鈕換行可觸及。
- 選 9/16 後當日課程可見；取消 A 後同堂 B 保留，取消歷史預設收合。
- 教練展開 18:00、報到、完成、收合後仍保留 9/16 及原課程位置。
- 使用獨立瀏覽器擷取 390px 假資料圖，公開於示意目錄；不混用真實資料截圖。

尚未進行正式功能頁手機改版，也不以本次示意操作取代實作後完整共卡、釋放、扣點、防重送及實體手機／iPad 驗收。等待使用者確認版面；不合併、不上線正式站。

## 第二輪手機示意調整

仍只改隔離示意；`legacy.js` 保存前輪原稿整理／目前版轉錄，沒有把新設計回寫成原稿。

### 沿用與新增

- 沿用已找到的會員設計依據：完整月份、本人／共卡標記、同堂合併與逐人取消。
- 本次新增：下一次上課精簡摘要；每方案分列可用點數／到期日；課程列教練、點數、剩餘名額；不預選上課者；選人→確認學員／方案／總占用→送出的兩步流程；課前重要提醒直接顯示。
- 教練原稿未找到，依既定需求整理：今天／回到今天、下一堂、教室、待報到優先、展開後固定學員順序、報到／完成／未到原列更新、取消學員收合。
- 修正版可選本人、只替 B、同堂多人、無預約、滿班、點數不足、首次送出失敗後重試，全部是假資料。
- 同為 9/16，會員示意時間 17:50、教練 18:05，以示範上課前預約與開課後點名；這不是更動正式時間規則。

### 本輪示意操作檢查

- A/B/C 一開始全未勾選；只選 B 後確認頁顯示 B、共卡二十點及總占用 3 點。
- 首次送出模擬失敗，選擇與備註保留；再次提交成功只新增一筆 B。送出期間停用確認、返回與情境切換，顯示送出中。
- 成功後直接顯示該課次，下一次上課摘要更新；取消確認指明學員且不取消整組。
- 滿班預約停用；可用 1 點選 3 點課，顯示不足原因、保留 B 並停用下一步。
- 教練展開 18:00 時順序 B（待報到）、A（已報到）；報到後仍 B/A，完成 B、未到 A 後仍原列。取消 C 留在收合歷史。
- 375、390、430px DOM 寬度檢查沒有整頁水平溢出。390×430 縮短可視高度時，面板內容可捲動，底部按鈕範圍為 y=361–430，可觸及。這是鍵盤空間模擬，不代表實體 iOS/Android 鍵盤已驗收。
- 選日期不再強制 scrollIntoView；模式／身分切換保留月份及日期，另有明確「回到今天」。

此輪的占用、扣點與釋放僅為記憶體中的示意數值，不能作為真實交易驗收。等待版面確認後才套入功能頁並重測既有真實流程。
