# 課程承接成熟架構對照（2026-09-17，進行中）

基準：課程分支 4a378bd6，main e69323f4。保留課程專屬資料與交易服務；正式蒸足頁面僅唯讀盤點，所有寫入驗收只在隔離預覽。

狀態定義：已存在≠本輪驗收通過；「待接」不算完成。受權限／店家功能控制的項目必須在後端重驗。隱藏頁不作成熟可用功能的證據。

## 頁面 → 操作 → 服務 → 課程差異與承接

|蒸足頁面／區塊|按鈕、側窗、子頁及開關|服務／資料|課程現況與缺漏|共用方式／必要差異|本輪結果|
|---|---|---|---|---|---|
|首頁 dashboard|現金開店／結帳／收支、今日預約名單、待辦、快捷入口|cash-drawer、booking、提醒|已有課程當日與現金抽屜；逐項核對排列與待辦|共用現金抽屜，名單保留 CourseBooking|待驗|
|customers 清單／詳情|搜尋、狀態／到訪／推薦／人員篩選、分頁、drawer、資料修改、方案／購買／預約、健康、匯出、合併子頁|queries/customer、customer actions、CustomerIdentityLink|已共用成熟篩選／清單／歸屬表單，接課程購買及上課；批次、匯出、合併仍待完整適配|抽出成熟清單／drawer 展示，課程方案與預約由 course queries 注入；不套蒸足 wallet/booking|待接|
|staff|角色、基本資料、啟停用、分組權限、帳號、排班|staff、course-staff、StaffMemberLink|已有共用 staff-workspace 與角色連結|沿用現有，排班對照課程可授課時段；不授總部權限|待驗|
|health、customers/id/health、前台 health/new|新增／編輯量測、摘要、完整量測、歷史／趨勢、已開通分析|HealthRecordForm、HealthAssessmentCard、native-health-service、健康功能 gate|已接共用完整表單、歷史／趨勢及功能 gate；前台本人資料操作待登入實測|共用成熟量測表單與摘要，courseMember 只解析本人，不用共卡查健康|待接|
|plans／購買|建立、編輯、上下架、適用課程、期限、指派、共卡、銀行資訊、訂單／核帳／發卡|course-members、course-portal、CoursePointCard／Purchase|核心已驗，待完整子頁／錯誤／排序對照|保留 Course 點數、占用及快到期優先；不能直接用 Steamfoot WalletSession|待驗|
|revenue 營運|營收明細、現金收支側窗、現金管理、KPI、交易工作台、日期／類型／人員篩選、明細側窗、修改／作廢／退款、匯出|queries/transaction、report、TransactionDrawer、cashbook|已接 revenue 工作台、課程核帳／退款／作廢／歸屬更正；完整並行與共用回歸待補|抽出成熟營運布局；資料與操作注入課程交易 adapter；退款受使用／占用狀態約束|待接|
|營運相關子頁|store-revenue（月季年）、reconciliation、data-export、transactions|報表、對帳、匯出服務|收入總覽／Excel 與對帳中心已适配；通用 data-export／transactions 尚未視為課程入口|課程購買和 Cashbook 去重；不能把同筆核帳收入加兩次|待接|
|reports 分析|今日／本月／自訂、營運摘要、客流、成交、六個月趨勢、留存、營收／店長分析、同期比較、查看顧客、全店／店長 CSV、月結|customer-flow、conversion、retention、performance-trends、snapshot|已接成熟日期、摘要、客流、趨勢、留存及教練量；手動收支與店長分析等尚待接|共用成熟 report 展示與日期；替換課程查詢、快照命名／統計口徑，無來源不可回填假 0|待接|
|settings 控制台|分類導覽、狀態卡、編輯／次入口、快捷操作、系統資訊|SettingsShell/NavSection/ActionCard/SidePanel|已共用控制台、同頁店家規則／付款；其餘子頁尚待逐項適配|共用成熟控制台；逐個放行已適配子頁|待接|
|settings/hours、duty、payment、plans|營業／公休／特殊日、預約開放日、排班啟用、銀行、成長方案中心|ShopConfig、BusinessHours、SpecialBusinessDay、Duty|付款與真實方案用量已接；營業／公休與值班寫入 adapter 待接，plans 靜態示意不列完成|公休標記及可約日期用同店既有設定；實際課次仍由 CourseSession|待接|
|settings/trial、referral-share、digital-butler|體驗价格範圍、分享模板、流程編輯、名單|trial settings、referral、digital-butler|課程尚未適配|逐项檢查現有模板／事件目的地；涉及新優惠／獎勵規則需明列，不自動套用|待接|
|reminders、growth、操作指南|規則／模板／開關、觸發事件、對象、發送紀錄、重試、深層連結|通知工作流、顧客經營查詢、指南 catalog|課程指南目前關閉，提醒／經營路由阻擋|通知僅測試授權對象；Course 事件與方案，不用蒸足餘堂觸發條件|待接|
|LIFF 會員／教練|既有底部導覽、日曆、共卡、同堂逐人、原位名單、點名／更正／批次、健康／店家|course portal/actions、course booking|已驗核心保留；健康／資料子功能未完整；檢查桌機手機寬度和操作文案|保留已定案版面，套共用功能而非重畫前台|待接／驗|

## 真實頁面唯讀盤點

2026-09-17 已開啟蒸足預約、營運、分析、設定實際頁面。營運確有營收明細／現金收支／完整現金管理、交易工作台、匯出、收入總覽、對帳中心；分析確有日期切換、客流／成交／留存、CSV／月結；設定確有分類與多個編輯子頁。未在正式頁按下寫入、退款、通知、匯出或核帳。

`dashboard/ops/page.tsx` 開頭無条件 redirect，後續程式是保留的隱藏頁，不得冒充目前成熟營運入口；實際入口是 revenue。其他隱藏區塊以條件、feature gate、權限逐項記錄。

## 必要商業差異

- 蒸足退款 pure helper 以 WalletSession 堂數與單堂價格計算；課程以點數卡、多課程不同額度運作。不能直接套用。已詢問部分使用點數卡的退款金額規則，等待回答；未使用且無占用可沿用全額退的既有語意。這不阻擋其他承接批次。
- Customer 緊急聯絡已新增獨立 nullable 欄位，僅隔離庫套用，列入正式發布方案；原資料未回填，不以 serviceNote 混存。
- 課程購買核帳目前記入 CashbookEntry，不寫蒸足 Transaction。營收、退款、對帳須以同一課程來源對齊，不能混讀其他模組帳。

## 驗收記錄

尚在盤點／實作，沒有將以上待接功能標為驗收通過。後續每批附程式、資料與真實瀏覽器證據，故障修復後再驗受影響流程。

### 批次一：健康共用（4b34d2b2）

- 共用成熟 HealthRecordForm／HealthAssessmentCard，包含九項量測、日期、備註、历史、趨勢；課程後台與會員使用同一工作區。
- 新增店家健康功能 gate；會員 action 固定使用登入者本店顧客，不接受共卡他人作為健康資料對象。
- 隔離預覽後台實際新增一筆本轮健康量測，再編輯同筆重量；SQL 驗證紀錄仍只有一筆且店別正確。沒有更動既有紀錄。
- 型別／修改檔 lint、健康及成熟表單相關 16 tests 通過。會員實際新增／編輯等待可操作視窗登入，未列為通過。

### 批次二：營運與未使用退款（實作／驗證中）

- 原側列營運僅導向現金帳，已改接 revenue 課程資料 adapter；共用 RevenueTabs、CashbookShortcut、DataTable、KpiStrip、SideCard、RightSheet。
- 已寫入購買日期／核帳人員／狀態篩選、分頁、購買明細、核帳與全額退款入口。摘要依核帳／退款日期，不把課程購買與現金帳連動重複相加。
- 全額退款採原購買實付；占用、部分使用、額度異動、額外贈送、原收款不符先擋。退款、收回額度、卡片結清、收支、稽核在同店鎖與同一交易完成；請求重送不重退。
- 退款後禁止出席更正重新增加額度。連動現金帳不可單獨改額／刪除。
- 完整交易更正／作廢、收入總覽、對帳、匯出與其他成熟功能仍待接，**本批不等於營運整頁已完成**。
- 部分使用退款依 `course-refund-rule-comparison-20260917.md` 待商業規則確認，沒有自行套用公式。

2026-09-17 本批隔離瀏覽器結果（ecea5f5f）：以本輪新建、明確標示的待核帳測試訂單（非真實銀行付款），實際在營運側窗核帳發卡，再填原因送出未使用全額退款。UI 顯示退款成功，SQL 核對：REFUNDED、remaining=0、closed=true、退款單/REFUND額度紀錄/稽核各一筆；原收入與退款支出淨額為0。既有購買與顧客資料未改。此結果不代表部分退款、並行退款或完整營運承接驗收通過。

同批追加：承接既有對帳中心完整介面／歷史與 Debug，替換為同店課程核帳、退款上限、額度、占用與容量五项檢查。隔離 SQL 五项無差異；網頁手動執行仍待驗。交易更正可改備註／同店人員歸屬且連動現金帳並稽核；作廢仿照成熟流程要求整卡未使用、無任何預約或額度異動，保留原交易與原日非現金沖銷。86項本批相關測試通過，未以此取代真實操作。收入總覽／匯出與其他待接項仍未完成。

2026-09-17 真實瀏覽器（3c411979）：新建另一筆明確標示的隔離訂單，先在同頁更正備註、核帳發卡、再作廢；後台保留備註及作廢原因。SQL 核對 VOIDED、卡片0/closed、VOID額度與作廢稽核各1筆、原收款備註同步、沖銷淨額0。原有訂單未操作。對帳中心手動執行成功，5項檢查0差異；發現共用表格不宜將已檢查筆數當成比對基準，已分開呈現，待部署重驗。

收入總覽／匯出目前實作：共用 RevenueReportClient（查詢、明細分頁、Excel匯出），課程 adapter 僅讀同店購買／退款，RepeatableRead 快照避免跨時點混讀；點數／堂數方案篩選，去重購買人數和核帳／退款發生日。不同模組資料不混用。UI 移除蒸足體驗／票券等不適用分類，保留課程實付額與真實退款；匯出專用課程欄位。5項資料adapter測試／型別／修改檔lint通過，網頁及匯出下載驗證待完成。

2026-09-17 真實瀏覽器（2ccc450d）：收入總覽月報核帳 3,300、退款 2,300、淨額 1,000；切日報後核帳／退款各 2,300、淨額 0、購買 1 人，作廢單未計入。實際點擊匯出並讀取下載 Excel，總覽數值一致，明細僅當日核帳 +2,300 與退款 -2,300。發現 KPI／付款說明仍沿用蒸足文字及明細原價／折扣缺少課程歷史快照，已改為課程統計名稱並移除無資料來源欄位（保留真實收退款），不以假 0 表示折扣。

09:03 隔離瀏覽器重新手動對帳：5項通過、0差異／錯誤，涵蓋作廢後5張卡；已檢查數與異常數分開顯示，歷史紀錄保留。

### 批次三：顧客成熟清單（部署驗證待完成）

共用 CustomersToolbar／CustomersTable，保留搜尋、LINE／階段、上課、推薦、直屬店長、排序、分頁與同頁側窗。最近上課僅由本店 CourseBooking 已完成出席取得；可用點數／堂數分開，扣除占用、排除到期／結清卡，共卡不當作每人獨立餘額。保留健康、教練連結、方案與預約入口，補地址及一般顧客備註，與後台限定服務備註分欄。篩選保留URL及捲動。20項相關測試、型別與修改檔lint通過；不代表顧客完整承接完成，緊急聯絡、歸屬編輯、完整購買歷程等仍待接。

顧客批次隔離瀏覽器（630672e0）：建立本輪專用顧客，填寫地址／一般備註／後台服務備註，搜尋後同頁重開並修改地址；儲存後搜尋仍保留。唯讀 SQL 確認三欄獨立、店別正確、未建立登入帳號；原有顧客未修改。清單共卡 A/B 各可見共用4點，並明示額度共用，最近上課來源為課程出席。

追加同頁購買／核帳／退款／作廢歷程，以 customer.read + transaction.read 雙權限及同店顧客查詢，使用訂單實付快照。按需展開，錯誤可重試；不載入蒸足 Transaction。預約歷程依卡片單位分開點／堂。8項篩選及權限／店別測試通過，型別與修改檔lint通過；新版歷程瀏覽器驗證待完成。

顧客緊急聯絡姓名／電話：新增独立 nullable 欄位，不混存備註、不回填原資料；後台資料表單接入同店 customer.create/update 權限。十份遷移的本機原子提交／失敗回滾演練通過，僅隔離庫套用，正式庫未變更。前台編輯與新版實際儲存驗證仍待完成。

會員端緊急聯絡表單放在既有「我的」帳戶區原位展開；只提交姓名／電話，後端 courseMember 固定本人與店別，不接受表單指定顧客。跨店／共卡冒用、停用會員、失效會員不重建及清除欄位測試通過，相關18項測試通過。實際會員操作仍待既有測試帳號登入，不宣稱通過。

實測顧客歷程（aece461a）：本月上課篩選只列有課程出席的顧客，購買側窗讀得原實付快照、退款與作廢原因。發現購買／預約元件重複使用同一 React key，更新後產生重複區塊與殘留讀取文字，已改不同鍵並待部署重測。全量 CI 的 reports-view-mode 單一失敗確認為新增模組查詢缺少測試替身，補實際模組查詢邊界及課程跨店檢視限制測試，4項通過；全量重跑待結果，未列為豁免。

本機全量回歸：533個測試檔／4,760項通過；3個 PostgreSQL 專用檔／32項因本機未設定測試連線而跳過，不能算通過，另由 postgres-integration CI 執行。報表 view-mode 失敗已修正並加入課程被檢視店別測試。iPad 1024×768 實測發現清單右側操作需要水平尋找，已補共用表格可選固定右欄，僅課程清單啟用並加大觸控按鈕，待部署重驗。

顧客批次重測：0ad00508 購買歷程只渲染一份、完成後無殘留載入文字；原購買／退款／作廢記錄分別保留。新建驗收顧客的緊急聯絡姓名／電話於後台實際儲存並唯讀核對同店資料庫一致。4238026e 在 1024×768 下 document scrollWidth=innerWidth=1024，右側查看按鈕範圍917–963px、高44px，固定右欄可操作。該提交 CI 型別／修改檔lint／完整Vitest／Targeted／postgres-integration 均通過；Cloudflare既有豁免。

顧客歸屬追加：抽出蒸足 CustomerAttributionForm，原頁預設維持原 action；課程採專用 adapter，需 customer.assign，只可選同店啟用中的後台店長，推薦人僅同店未合併顧客，不賦予教練／總部權限，也不觸發蒸足獎勵。儲存失敗保留選取，搜尋錯誤明示。相關37項測試通過，涵蓋跨店／停用或教練指派／自己推薦自己／權限拒絕／遮罩電話；網頁驗證待部署。

顧客歸屬實測（0ddaea42）：本輪自行建立顧客在側窗搜尋推薦人（電話遮罩）、選店長並儲存，清單即時更新。唯讀 SQL 確认歸屬店長為同店 OWNER、推薦人同店、未建立登入帳號；原顧客與身分綁定未修改。該版型別、lint、完整 Vitest、PostgreSQL CI 通過，Cloudflare維持豁免。本機完整測試 4,767 項通過／32項連線測試跳過，後者由獨立 PostgreSQL CI 補驗。

### 批次四：分析資料與成熟報表操作（驗證中）

共用 ReportDateRange、PageShell／PageHeader／KpiStrip／DataTable、TrendChart；課程查詢保留 CourseSession／Booking／Card，購買與退款直接沿用已驗證收入報表 adapter。今日／本月／自訂期間保留 view 參數，客流依實際上課者去重、首次完成日期判斷新舊客；參與人次、出席、點數／堂數分開。前期採相鄰同長期間（明示日期），去年同期採日曆年對應、閏日縮至月底；近六個月趨勢截止所選結束日。查看顧客受 customer.read，金額受 transaction.read，CSV 受 report.export／功能開通／既有限額與查看模式限制。

尚未視為完整分析承接：現階段先接上課／購買／回流／教練量與匯出；課程沒有體驗成交歸因時不給假0或假開卡率。蒸足空間費月結不適用課程，教練結算方法未約定，不套既有空間費；手動收支與更完整店長業績分攤仍須核對承接。網頁與CSV驗證待部署。

分析實测（f42533ce → 2a8d778e）：月報7堂、6參與人次、5完成／2人、使用13點／0堂，與唯讀 SQL 聚合一致；購買／退款／淨額與收入總覽一致。今日切換正常。自訂日期首次操作發現受控日期欄位沒有送出畫面值，修成原生表單 FormData，新增3項回歸，隔離重測成功保留 view=analytics 並套用單日；月／日不同統計數正確。CSV點擊已觸發瀏覽器下載事件，但內建瀏覽器沒有提供下載檔路徑，檔案內容仍未實際讀取，不列為完整檔案驗證。1024px無整頁水平溢出。另發現無退款日顯示 -0，已修成非負退款合計並補回歸。

### 批次五：設定控制台與付款權限（驗證中）

共用 SettingsShell、SettingsNavSection、SettingsActionCard、SettingsSidePanel；店家資訊、課程規則與付款採同頁 RightSheet。PaymentSettingsForm 加窄側窗配置與可注入服務，原蒸足預設 action 不變；課程 wrapper 先檢查 plans.edit、同店及啟用人員，再呼叫成熟 updateShopBankInfo。店家與預約規則仍走課程交易。舊合併表單僅要求 business_hours.manage 卻可改銀行，現補 plans.edit 與完整欄位檢查；一般設定不提交或更新銀行。查看模式不提供修改按鈕。

此批不代表完整設定已承接：營業公休、值班聯動、方案中心、提醒、推薦分享與數位管家子頁仍須逐項適配。既有 business-hours 更新會查蒸足 Booking、duty 以蒸足 slot 為基準，不能直接接入課程當作完成。

設定實測（750bfe23）：1024px 同頁開啟付款與店家規則，原值儲存後唯讀 SQL 確認隔離銀行三欄及原預約／取消分鐘均保留，店家表單顯示已儲存。付款複製按鈕未取得剪貼簿一致證據，仍列未驗證。該版型別、修改檔 lint、完整 Vitest、postgres-integration CI 通過；Cloudflare 維持豁免。分析單日重測確認無退款顯示 NT$ 0，不再出現負零。

方案用量追加：共用 getStoreUsage 原本只計蒸足 Booking，現依 COURSE 改讀同店 CourseBooking，以與課程額度限制一致的台灣月份／建立日計算，取消仍計入，未更動上限。設定用量摘要受 plans.edit 控制，不搬入既有 plans 示意頁的假續約資訊。三項資料來源／跨月邊界測試通過。另修正關閉設定側窗後的殘留控制項，以及共用報表切換日期後表單預設值重置。部署與新版用量畫面驗證待完成。

用量／側窗隔離實測（a77b8836）：頁面顯示啟用人員4、顧客4、本月課程預約15，唯讀 SQL 三項一致。實際開啟設定側窗有1個 dialog，關閉後為0，設定主頁與資料保留。此版 Typecheck、Changed-file ESLint、Targeted、Full Vitest、postgres-integration 與 Vercel 均通過；Cloudflare 維持豁免。本機完整測試4790通過，32項連線測試跳過已由獨立 PostgreSQL CI 補驗。

### 批次六：營業／公休承接（部署實測待完成）

重用 ScheduleManager 月曆、單日設定、多段營業、每週規則、預覽確認及後續週次操作。課程採專用 actions，BusinessHours／SpecialBusinessDay 同店讀寫；不使用蒸足 Booking、BookingSlot 或 SlotOverride，名額仍屬於課次／教室。課程不顯示單一蒸足時段容量或同步蒸足總部設定；原蒸足／SPA 預設動作保留。

日期批次与每週設定放在課程同店交易鎖內，更新後核對受影響未取消的未來課次，任一衝突整筆回滾；不移除課程／預約／點數。新增、複製及修改課次同樣檢查營業／公休，避免設定與排課並行繞過。每週修改保留特殊日，單日回復移除該日覆寫；原位操作保留月份。舊蒸足營業寫入動作對課程店明確拒絕，防止繞過課程檢查。

本機完整4797項通過／32項連線測試跳過；新增7項涵蓋店別、權限、特殊日優先、多段時間、公休衝突與整批回滾傳遞。舊營業服務追加隔離保護後32項相關回歸通過。型別與修改檔lint通過。尚未以此視為真實瀏覽器驗收完成，預約開放期限與提醒仍待下一步接入。

營業隔離實測（f90f59bc）：已有課日期設公休後未寫入特殊日；無課日期新增本輪公休成功，月曆顯示「休」、日期与原因保留，再由同頁回復預設。唯讀 SQL 確認特殊日回到原先0筆、原7堂課及15筆預約保留。原版 toast 短暫消失不利辨識失敗，已補持續可見的錯誤區，待部署重測具體衝突文案。此版型別／lint／完整與Targeted／postgres-integration／Vercel均通過，Cloudflare豁免。

### 批次七：課程上課提醒（實作與隔離驗證中）

- 沿用成熟提醒編輯卡、綠金 LINE 卡片、發送紀錄搜尋／對象／狀態／日期篩選及分頁；課程入口為 `courses/reminders`。課程通知獨立規則，原蒸足提醒引擎排除這類規則。
- 沿用前一日台灣時間 18:00；只查本店明日 RESERVED 且未取消課次，收件人是實際上課顧客，不是共卡操作人。授權店家、LINE 功能、月發送額度及驗證過的 LINE 身分皆保留。首次儲存文字不自動啟用通知。
- 主／備援 cron 納入課程引擎及掃描／成功／失敗總數。每次課程通知以店別、預約、上課時間建立固定識別碼，SENT 跳過；外部送出使用同一 LINE retry key。課程取消及規則停用在鎖定店家後重驗。
- 深層連結到同店會員專區的該日「我的預約」，不產生蒸足改期／取消網址。沒有改動已驗過的預約、扣點、取消與 LINE 綁定。
- 預覽環境維持全站禁止外發，SKIPPED 清楚標示未送達。程式測試不是 LINE 實機驗收；尚未驗證真實外發，也未將餘額／到期／店長事件提醒列為完成。
- 11 項課程提醒服務／權限／收件人／重送／限制測試，以及 cron 真實 handler 的合併統計、批次失敗、重試與認證測試通過。全庫第一次測試僅一項舊 source-contract 因統計改為含課程而失敗，已更新該斷言並新增 handler 行為測試，受影響測試通過；其餘 4808 項通過、32 跳過。
- 第十一份 MessageLog 課程連結遷移僅套隔離庫；完整本機 PostgreSQL 演練再次通過，含跨店預約／方案外鍵拒絕、混用舊預約欄位拒絕及整批回滾。正式庫未變。
- 營業／公休補驗：54f0262d 預覽實際將有課的 09/18 設公休，頁面持續顯示「2026-09-18 11:30 課程與營業／公休設定衝突，本批尚未儲存」。選取日期與輸入保留；SQL 確認特殊日仍 0、原有效課次 7、預約 15。批次公休及提醒新頁瀏覽器驗收仍待完成，原 IAB 連線在後續操作逾時，不以此宣稱通過。

### 批次八：營業設定的會員預約開放期限（待真實頁面驗收）

- 共用成熟 BookableUntilForm，支援指定日期含當日、精確 24 小時滾動 1–90 天，預設沿用既有 14 天。只限制會員新增預約，店長代約維持原成熟流程例外。
- 新課程 adapter 在同店交易鎖下確認期限外沒有 RESERVED 課程預約，再更新同店 ShopConfig。縮短期限有衝突則拒絕，不改預約、課次或點數。
- 會員課表顯示「尚未開放」，後端同樣以成熟 resolveCustomerBookingWindow 檢查，拒絕時不建立預約或額度占用。舊 shop action 若收到課程店請求會分流至課程 adapter，不查蒸足 Booking。
- 新增截止邊界、店長例外、縮短衝突、店別／權限、非法日期與天數測試，相關課程及蒸足設定回歸通過；型別與修改檔 lint 通過。尚未把這些結果當作瀏覽器操作驗收，頁面儲存、會員對照及觸控仍待補。

### 批次九：店長通知承接（程式完成，隔離操作待補）

- 共用成熟通知收件人清單、搜尋、狀態／角色篩選、綁定、啟停用、收合偏好及發送紀錄。課程目前接入「當日新課程預約」與「待核帳方案購買」兩個真實事件；尚未接入的 VIP／體驗／管家／未完成服務／摘要不列為已完成或顯示可用開關。
- 課程店不執行蒸足 legacy 收件人自動移植，不以 staff／教練／顧客的 LINE 帳號直接授予店長通知。管理操作仍需固定店別、有效店長權限及 LINE 功能開通。
- 事件在預約／購買提交後處理，LINE 失敗不撤回已完成交易；沿用共用事件唯一鍵、收件人偏好、停用重驗與 retry key。購買提醒不含匯款後五碼，連到本店課程待核帳；當日提醒清楚區分上課者與預約人。
- 蒸足預約提醒及蒸足方案到期引擎明確排除 COURSE 店，避免舊規則／舊錢包混用。課程到期與餘額提醒尚待承接，不以舊引擎代替。
- 新服務與既有通知／設定回歸通過；全庫第一次執行發現一組教練測試受新增 eager import 的 Next Auth 測試環境相依影響，已改為提交後才 lazy load 通知服務，該組及課程交易相關測試重測通過。真實 LINE 外發未驗證，隔離預覽外發封鎖維持。

### 批次十：分析手動收支與交易歸屬（程式完成，待頁面驗收）

- 沿用成熟分類表格與摘要，加入手動收入／支出、總收入、收支淨額、前期比較與店長交易歸屬；歸屬購買人數去重，退款依退款發生日，手動收支依登錄日期。收支淨額不宣稱會計利潤。
- 排除課程購買／退款／作廢已連動的 CashbookEntry，避免重複計入。transaction.read 與 cashbook.read 分開控制，缺權限時不查對應資料、不顯示誤導的部分合計或假 0。CSV 同步採相同口徑與權限。
- 新增加總、退款、歸屬去重、店別、日期與匯出權限測試，12項通過；型別與修改檔 lint 通過。隔離庫唯讀核對本月手動收入250，未修改任何帳列。新頁真實瀏覽器與下載檔驗證仍待補。
- 前批 6372d743 的 Typecheck、Changed-file ESLint、Targeted、Full Vitest、postgres-integration 及 Vercel 均通過；Cloudflare 依既有豁免。瀏覽器驗收控制仍曾出現逾時／點擊無反應，已重連、換頁與重新載入並請使用者確認手動展開；不將部署成功視為操作通過。

目前對照表狀態補充：營業／公休、預約開放期限、課程上課提醒、店長當日預約／待核帳通知與紀錄已接入。手動收支及店長分析已接入、待實測；值班、到期／餘額提醒、其他顧客經營與指南仍待承接。前述各批的「待接」為當時狀態，不代表上述後續成果被移除。

### 批次十一：課程操作指南（程式完成，待頁面驗收）

- 共用成熟指南搜尋、分類、側邊閱讀及全部教學頁；課程新增六篇有來源的共卡代約、逐人取消、報到／完成／未到、公休／期限衝突、未使用全額退款與提醒说明。
- 依模組、操作權限及 LINE 功能篩選文章；課程不同 view 及子頁可取得對應常見問題，不引用蒸足改期或補課券文章。不把尚未核准的部分使用退款寫成可操作功能。
- 指南相關18項測試及型別通過；修改檔 lint 0錯誤，sidebar 原有一項 useMemo 多餘 routeQuery 依賴警告仍保留。搜尋、側窗捲動、窄螢幕與前往完整指南仍待真實操作驗證。
- 318f2e55 分析承接 CI 型別／lint／Targeted／Full Vitest／postgres-integration 與隔離部署均通過；Cloudflare維持豁免。

### 批次十二：顧客 CSV 匯出（程式完成，下載實測待補）

- 共用既有顧客匯出入口與功能開通檢查，課程分流到同店專用查詢，不查蒸足錢包或預約。清楚標示「匯出全部顧客」，不冒充僅匯出目前篩選結果。
- 內容含基本資料、緊急聯絡、一般備註、逐卡餘額／占用／可用／到期、點數與堂數分列、本人未取消預約及完成出席人次。共卡明示不可跨人加總，不把不同方案效期合併。
- customer.export 與 customer.read 雙權限、查看模式阻擋；wallet.read／booking.read 分別控制是否查詢與輸出。CSV 文字防試算表公式注入，匯出不含登入帳號、健康量測或後台服務備註。
- 權限、店別、模組、共卡、到期、占用、日期及原匯出 gate 相關8項測試通過；型別與修改檔 lint 通過。首次型別檢查發現測試 BigInt literal 不符合既有編譯目標，改為 BigInt() 後重測。尚未宣稱實際下載檔驗收通過。
- 9216a431 指南 CI 型別／lint／Targeted／Full Vitest／postgres-integration 與隔離部署通過，Cloudflare豁免維持。
