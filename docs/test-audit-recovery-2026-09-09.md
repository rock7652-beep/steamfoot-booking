# 測試盤查與修復紀錄（2026-09-09）

本次在獨立分支 `fix/test-audit-recovery` 完成修復，基準為保留的 main 提交 `fb19046911e45fda139845d180d550b0a3ca1e2c`。未推送、部署或合併正式站；此結果不代表最新正式站已套用修復。

## 提交

| 提交 | 內容 |
| --- | --- |
| `4406ab8` | 修復 33 個測試檔的依賴、資料模擬及舊規則斷言，保留並加強錯誤原因、跨店、扣堂與唯讀檢查。 |
| `9422108` | 補齊會員身分測試依賴，修復跨店身分查詢例外時沿用上一店顧客 ID 的問題。 |

## 確認的程式缺陷

顧客 session 帶有 A 店顧客 ID，但目前請求為 B 店時，若會員身分查詢拋出例外，原本的 catch 會回傳 A 店 session。新增案例已先在原程式重現失敗。

修復後，已解析到 B 店的跨店請求遇到查詢例外，會清除顧客 ID，並保留 B 店請求範圍，交由既有顧客入口檢查處理。同店暫時查詢失敗仍維持既有行為，另有案例驗證。

此修復只處理請求中的 session 結果，不修改會員綁定、正式資料、預約、收款或扣堂程式。

## 測試修復範圍

- 接回前次舊工作目錄留下、尚未提交的測試修正。
- 補齊新版權限、React/Next 快取、可預約日期、身分解析、通知服務等測試依賴。
- 跨店預約與報表測試使用實際授權函式搭配有限的母子店資料，新增無關門市遭拒且不查詢預約的驗證。
- 校正與現有程式不一致的分享文案、LINE 快捷按鈕、門市設定、付款連結及 OAuth 完成流程斷言。
- 校正分析為獨立加購的測試，並讓資料匯出測試先滿足分析開通條件，確實測到匯出權限拒絕原因。
- 保留無效方案、堂數不足、保留堂數不符、唯讀及權限不足等拒絕案例；沒有新增 skip 或刪除失敗案例。

## 驗證

| 檢查 | 結果 |
| --- | --- |
| `vitest run` | 435 個測試檔；3,918 項通過、0 項失敗、31 項原有條件式跳過。 |
| `tsc --noEmit --incremental false` | 通過，無診斷輸出。 |
| 變更檔案 ESLint | 0 個錯誤；11 個既有未使用參數警告，位於 LINE webhook 測試。 |
| `git diff --check` | 通過。 |
| 跨店例外回歸 | 修復前新增案例失敗；修復後跨店例外及同店例外案例均通過。 |

31 項未執行案例分別為：

- `booking-actions.pg.integration.test.ts`：13 項。
- `booking-slot-concurrency.pg.test.ts`：5 項。
- `weekly-recurring-bookings.pg.integration.test.ts`：13 項。

這些案例需要獨立 PostgreSQL 測試資料庫，本次環境未配置；沒有改用正式資料庫執行。瀏覽器、Preview 及正式環境驗收未在本次進行，因此不宣稱完成部署驗收。
