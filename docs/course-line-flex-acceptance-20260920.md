# 課程單則 LINE Flex 隔離驗收（2026-09-20）

## 已完成

- 依明確授權啟用指定測試官方帳號 Messaging API，加入指定既有 Provider；未更改中央 Login 的官方帳號關聯、客服中心、圖文選單及其他店家設定。
- 本店 bot info / basic ID / destination 比對成功。新憑證只在 Vercel 此開發分支 Preview 設定，未下載或公開憑證。
- Webhook URL 設為固定隔離預覽 `/api/line/webhook`，LINE Console Verify 回傳 Success；OA Manager Webhook 開關回讀為 1。這只證明驗證端點可達，不代表全部 follow / unfollow 等真實事件驗收完成。
- 經一般店長介面建立次日有效課次及本人預約，使用正常方案保留額度。先前已取消預約未挪用，A 店方案、期限、人員及額度限制未變。
- 使用實際日期、現有課程提醒候選流程；隔離驗收限定一店、一筆有效預約、一名已驗證會員與 LINE 身分雜湊、固定 retry key、當日到期授權。
- 11:09:36（台灣）伺服器實測：`total=1, sent=1, skipped=0, failed=0`；資料庫 `SENT`、`STORE`、error=null。
- 同一請求重送：`total=1, sent=0, skipped=1, failed=0`，成功紀錄仍只有 1 筆，未再次呼叫 LINE。
- A 店原本关闭的課程自動提醒已恢復 false；未啟用其他提醒或群發。

## 實測發現與修復

首次執行在呼叫 LINE 前失敗：互動式交易已持有唯一連線，收件人載入器又使用全域 Prisma，10 秒後 pool timeout。日誌、執行順序及 FAILED 紀錄核對後才修復重測，未盲目重送。

`647a9b89` 讓兩個身分查核服務接受既有交易 client；課程提醒傳入同一 client。舊呼叫保持原預設。身份、店別、有效預約、鎖、月額度及防重送規則保留。

- tsc / 受影響檔案 ESLint 通過。
- 相關提醒、中央身分、既有路由、驗收授權 26 項測試通過；設定型路由含交易 client 回歸 6 項通過。
- 部署：`647a9b89`，Preview READY；未正式部署。

## 待驗證及界線

- 2026-09-20 使用者本人手機 LINE 確認：收到測試 Flex、文字與按鈕正常；按鈕返回 A 店「我的預約」，本人姓名、9/21 10:00 伸展瑜珈與提醒一致，黑色浮動工具列未出現。收件、呈現及返回驗收通過，不再重送。
- `14e494a0` 隔離部署 READY 後唯讀確認 `grantConfigured=false`，臨時外發授權已撤除；A 店自動提醒維持關閉。
- 此次是經 HQ 授權觸發真實服務、真實候選及真實 LINE 推送；不是 18:00 cron 自動觸發實機驗收，不冒稱該項通過。
- 未增加其他真實 LINE / Email 訊息；未重測已通過的手機登入、本人預約及取消。
- 不包含實體 Safari / iPad 新回歸。
- 公開紀錄不列收件人身分、預約 ID、測試入口或憑證。私有身分證據留在隔離驗收記錄。

## 9/30 剩餘關卡

1. 本次 Flex 收件、呈現及返回：使用者本人實機確認通過，已關閉此關卡。
2. 正式一致性邏輯備份的本機隔離恢復目標待核准；尚未匯出或恢復。詳細方案見 `course-recovery-target-plan-20260920.md`。
3. 正式發布授權、正式遷移及發布後檢查仍未執行；隔離演練不能等同正式發布。
4. Cloudflare 維持豁免，不能標記通過。

PR #1022 保持 Draft。未修改正式資料庫、未合併。
