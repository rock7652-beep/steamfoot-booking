# 第二批介面整理驗收（2026-09-19）

功能版本：e403f414。僅修改課程 workspace 與 course staff workspace；未改首頁、共用蒸足／SPA 元件、後端服務或資料模型，無新增遷移。

## 完成項目

- 課程：原列表與側窗保留，新增文字查閱與狀態；查閱再編輯。名稱、課型、分類、排課預設置前，介绍與注意事項置後。複製有獨立標題與建立按鈕。
- 教室：名稱、分類、容量置前，設備／位置／原備註同一個選填區，保留各欄資料。用啟用／停用詞彙。
- 人員：統一身分摘要，查閱用文字；純店長無授課分頁，店長才有兼任選項，纯教練無後台設定。後台帳號與權限分組；資格與會員工作連結分組。無課次顯示空狀態。非授課身分儲存保留既有資格資料。
- 排課：課程、教練、日期時間、教室容量、重複方式依序排列；時長調整收折，單一教室保留標籤。無資格教練時開新分頁設定人員，原排課頁保留草稿，返回按更新教練名單。此為保留既有草稿的實際方式，不宣稱同側窗嵌入人員編輯。

## 真實隔離網頁操作

使用既有一般店長登入隔離預覽，沒有使用 HQ 繞過權限。

| 項目 | 結果 |
|---|---|
| 課程文字查閱、狀態與編輯入口 | 通過；查閱不呈現輸入表單 |
| 新增課程及複製課程建立 | 通過；建立本輪專用資料，保留原資料 |
| 教室選填欄位唯一、儲存 | 通過；使用原值儲存，沒有覆寫為空 |
| 三種身分條件欄位 | 通過；新增介面切換純店長／兼任／純教練 |
| 既有兼任人員儲存重開 | 通過；資格勾選保留 |
| 未儲存關閉，選繼續編輯 | 通過；修改內容保留 |
| 缺資格入口開啟人員頁並返回 | 通過；日期與時間在更新教練名單後保留 |
| 排課容量檢查 | 過大容量被後端阻擋；調整為符合容量後建立成功 |
| 桌機、iPad 横向／直向尺寸 | Chromium 1440×1000、1024×768、768×1024；底部按鈕可见，中心 hit-test 未被浮層遮擋 |

實體 iPad／Safari 未測。本次新建純店長／純教練帳號的完整儲存未重跑，條件介面已驗證；既有後端身分、撤權、跨店與並行規則沿用前批證據。沒有把示意驗證算作真實功能驗收。

## 檢查

- TypeScript、兩個修改檔案 ESLint 通過。
- 本機課程排程、action、存取與第二批測試 31 項通過。
- 功能提交 CI：Changed-file ESLint、Typecheck、Full Vitest baseline、postgres-integration、Targeted tests 通過。
- Cloudflare Workers Builds 失敗，依既有約定豁免，非通過。
- Vercel Preview READY；沿用分支隔離資料庫檢查。無正式部署或資料遷移。

蒸足／SPA 共用元件、登入與交易程式未修改，沿用原驗收；本輪未重新進行蒸足／SPA 真實交易。
