# Speed Audit v1 — 環境變數範例（值留空，真實值請放本機環境，勿提交）
#
# 用法（擇一）：
#   1) 直接在指令前帶入：
#        SPEED_AUDIT_BASE_URL=https://your-preview.vercel.app \
#        SPEED_AUDIT_EMAIL=owner@staging.local \
#        SPEED_AUDIT_PASSWORD=*** \
#        npm run speed-audit
#   2) 或自建本機 e2e/speed-audit/.env.local（已被 .gitignore 的 .env.local 規則涵蓋）後 source。
#
# 規則：
#   - SPEED_AUDIT_BASE_URL 必填；缺少會 fail-fast。
#   - 命中 production domain（steamfoot.com）會直接 abort。
#   - 帳密請用 staging 帳號，禁用正式帳號。
#   - bypass token 只放本機 env / CI secret，切勿貼到 chat / PR / commit。

SPEED_AUDIT_BASE_URL=
SPEED_AUDIT_EMAIL=
SPEED_AUDIT_PASSWORD=
# 選填：搜尋用關鍵字（≥2 字），預設「測試」
SPEED_AUDIT_SEARCH_QUERY=
# 選填：Vercel Deployment Protection 通道（Protection Bypass for Automation）。
# Preview 有開保護時必填；值留空，真實 token 只放本機 env，勿提交。
# 相容 VERCEL_AUTOMATION_BYPASS_SECRET。
SPEED_AUDIT_BYPASS_TOKEN=
