name: Booking isolated database audit

on:
  pull_request:
    branches: [main, codex/course-scheduling-stage1]
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  postgres-integration:
    if: >-
      github.head_ref == 'fix/test-audit-recovery' ||
      github.head_ref == 'codex/course-scheduling-stage1' ||
      github.head_ref == 'codex/course-trial-retention-30-days' ||
      (github.event_name == 'workflow_dispatch' && github.ref_name == 'codex/course-scheduling-stage1')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:17.6
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: disposable-test-only
          POSTGRES_DB: steamfoot_audit_test
        ports: [5432:5432]
        options: >-
          --health-cmd "pg_isready -U postgres -d steamfoot_audit_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      DATABASE_URL: postgresql://postgres:disposable-test-only@127.0.0.1:5432/steamfoot_audit_test
      DIRECT_URL: postgresql://postgres:disposable-test-only@127.0.0.1:5432/steamfoot_audit_test
      BOOKING_INTEGRATION_TEST_DATABASE_URL: postgresql://postgres:disposable-test-only@127.0.0.1:5432/steamfoot_audit_test
      BOOKING_CONCURRENCY_TEST_DATABASE_URL: postgresql://postgres:disposable-test-only@127.0.0.1:5432/steamfoot_audit_test
      DIGITAL_BUTLER_ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Create schema in disposable database
        run: npx prisma db push --skip-generate
      - name: Verify retention boundaries and three upgrades on disposable PostgreSQL
        run: >-
          npx vitest run src/__tests__/trial-retention.pg.test.ts
          --reporter=default --reporter=json --outputFile=retention-pg-results.json
      - name: Require all retention database cases without skips
        run: |
          node --input-type=module -e '
            import { readFileSync } from "node:fs";
            const r = JSON.parse(readFileSync("retention-pg-results.json", "utf8"));
            if (!r.success || r.numTotalTests !== 3 || r.numPassedTests !== 3 || r.numPendingTests !== 0) {
              throw new Error("All three retention database scenarios must pass without skips");
            }
          '
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: retention-pg-results
          path: retention-pg-results.json
          retention-days: 3
      - name: Run all 32 PostgreSQL scenarios
        run: >-
          npx vitest run --config vitest.pg.config.ts
          src/__tests__/booking-actions.pg.integration.test.ts
          src/__tests__/booking-slot-concurrency.pg.test.ts
          src/__tests__/weekly-recurring-bookings.pg.integration.test.ts
          --reporter=default --reporter=json --outputFile=booking-pg-results.json
      - name: Verify no PostgreSQL scenarios were skipped
        run: |
          node --input-type=module -e '
            import { readFileSync } from "node:fs";
            const r = JSON.parse(readFileSync("booking-pg-results.json", "utf8"));
            if (!r.success || r.numTotalTests !== 32 || r.numPassedTests !== 32 || r.numPendingTests !== 0) {
              throw new Error("Expected all 32 database scenarios to pass without skips");
            }
          '
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: booking-pg-results
          path: booking-pg-results.json
          retention-days: 3
      # Course tests create/drop their own schema and invoke the real refund service.
      - name: Verify concurrent course refunds and atomic rollback
        run: >-
          npx vitest run src/__tests__/course-refund.pg.test.ts
          --reporter=default --reporter=json --outputFile=course-refund-pg-results.json
      - name: Verify no course refund scenarios were skipped
        run: |
          node --input-type=module -e '
            import { readFileSync } from "node:fs";
            const r = JSON.parse(readFileSync("course-refund-pg-results.json", "utf8"));
            if (!r.success || r.numTotalTests !== 3 || r.numPassedTests !== 3 || r.numPendingTests !== 0) {
              throw new Error("Expected all 3 course refund database scenarios to pass without skips");
            }
          '
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: course-refund-pg-results
          path: course-refund-pg-results.json
          retention-days: 3
      - name: Verify exact course trial migration and isolated payment / attendance
        run: >-
          npx vitest run src/__tests__/course-trial.pg.test.ts
          --reporter=default --reporter=json --outputFile=course-trial-pg-results.json
      - name: Verify no course trial scenarios were skipped
        run: |
          node --input-type=module -e '
            import { readFileSync } from "node:fs";
            const r = JSON.parse(readFileSync("course-trial-pg-results.json", "utf8"));
            if (!r.success || r.numTotalTests !== 6 || r.numPassedTests !== 6 || r.numPendingTests !== 0) {
              throw new Error("Expected all 6 course trial database scenarios to pass without skips");
            }
          '
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: course-trial-pg-results
          path: course-trial-pg-results.json
          retention-days: 3
      - name: Verify course trial activation concurrency without resetting existing dates
        run: >-
          npx vitest run src/__tests__/course-trial-activation.pg.test.ts
          --reporter=default --reporter=json --outputFile=course-activation-pg-results.json
      - name: Require actual activation test execution
        run: |
          node --input-type=module -e '
            import { readFileSync } from "node:fs";
            const r = JSON.parse(readFileSync("course-activation-pg-results.json", "utf8"));
            if (!r.success || r.numTotalTests !== 1 || r.numPassedTests !== 1 || r.numPendingTests !== 0) {
              throw new Error("Expected the concurrent activation database test to pass without skips");
            }
          '
